# Used for both the realtime voice session and the agent's underlying model
# (via Inworld's OpenAI-compatible router). Key is pre-Base64-encoded — pass
# verbatim, do not re-encode.
INWORLD_API_KEY=<your-pre-base64-encoded-inworld-key>

# Optional: Server port (defaults to 4111)
# PORT=4111
